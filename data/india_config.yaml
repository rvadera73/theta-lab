# India portfolio config — edit this file, commit + push to take effect.
# Script reads this at runtime; no Python changes needed.

# ICICI Direct symbols tagged with a "(core)" thematic label in the equity report.
# INFORMATIONAL ONLY as of 2026-07-19 — this list no longer determines the report's
# KEEP/WATCH/verdict (that's now a live conviction score in enhanced_metrics.py,
# recomputed every run: revenue/earnings growth + analyst rating as the primary
# signal, RSI/MACD/PE/52w-position as supplemental). This list is just the
# long-term thematic story for each name, and the theme set itself is now
# auto-checked every report run against real NSE sector index momentum
# (check_sector_themes() in india_us_evening_report.py) — see flags below.
#
# 6 growth themes as of 2026-07-19 (was 5 as of 2026-04-30 — Pharma added):
#   Power/Electricity | Defense | Hospitals | Real Estate | Value (Banking/Infra) | Pharma
#
# 2026-07-19 sector-momentum audit (3mo/6mo NSE sector index returns vs Nifty 50):
#   - Pharma sector +14.0%/+12.7% — outperformed 4 of the then-5 core themes while
#     being excluded entirely. Added as a 6th theme (see AURPHA below).
#   - Defense/PSU proxy (Nifty PSE) -6.1% 3mo (but +1.7% 6mo, so not yet a sustained
#     cooling signal) — softer than when the theme was set; auto-flagged COOLING in
#     the report if it stays negative on both windows in future runs.
#   - IT confirmed correctly excluded (-8.1%/-23.1%).
core_portfolio:
  - HINAER    # HAL — Defense (zero debt, 30% profit growth)
  - BHAELE    # BEL — Defense (zero debt, 20% profit growth)
  - MAZDOC    # Mazagon Dock — Defense/Navy (zero debt)
  - ADAPOW    # Adani Power — Electricity (92% profit growth)
  - NTPC      # NTPC — Electricity (fwd PE 14.9x, value)
  - APOHOS    # Apollo Hospitals — Healthcare
  - YATHOS    # Yatharth Hospital — Healthcare (46% revenue growth)
  - ADAPOR    # Adani Ports — Infrastructure (22% revenue growth)
  - RELIND    # Reliance — Conglomerate (telecom/retail/renewables)
  - HDFBAN    # HDFC Bank — Banking anchor (value)
  - BAJFI     # Bajaj Finance — NBFC (48% revenue growth)
  - DLFLIM    # DLF — Real Estate
  - ZOMLIM    # Zomato — Consumer/Food Delivery (growth)
  - AURPHA    # Aurobindo Pharma — Pharma (added 2026-07-19: sector outperforming,
              # conviction 7-8/10, no red flags — the pharma representative.
              # NOTE: DRREDD (Dr Reddy's) is NOT added despite the same sector —
              # its earnings collapsed -86% YoY and it's analyst-downgraded to
              # hold; that's a company-specific problem the strong sector doesn't
              # offset. See its live verdict in the report, not this list.

# Watchlist — target entry zones. Not yet purchased.
#
# KAYNES removed 2026-08-24 — validated via web research, not a rumor: Q1
# FY27 consolidated net profit -24.4% YoY, execution delays on the Sanand
# OSAT plant and Tamil Nadu PCB ramp-ups, persistent negative cash flow, a
# cut to smart-meter/semiconductor-packaging growth targets, and management
# withdrew specific FY27 revenue guidance (replaced with vague "twice
# industry rate" language). A real thesis break, not a temporary dip.
watchlist:
  - symbol: SOLARINDS
    name: Solar Industries
    sector: Defense / Explosives / Ammunition
    thesis: India defense indigenisation, ammunition demand surge
    # Re-anchored 2026-08-24: the STRATEGY (wait for the 50-day MA) is
    # unchanged from when this was first set, but the MA itself has moved a
    # lot as the stock ran (roughly 14,000 -> 18,567 over the last several
    # months) — the old 14,000-14,500 zone is now ~38% below spot and would
    # never realistically trigger. Fundamentals still support paying up
    # somewhat: revenue growth +70%, earnings growth +93%, analyst mean
    # target 21,521 (ABOVE current price, unlike PAYTM below) — this is a
    # real uptrend with a live 50-MA discipline, not a chase.
    entry_zone_low: 18000
    entry_zone_high: 18600
    current_price: 19925
    strategy: "Wait for pullback to the 50-day MA zone (re-anchored 2026-08-24 at ~18,000-18,600, was 14,000-14,500). Buy 5 shares."
    max_position: 10  # shares

  - symbol: PAYTM
    name: One97 Communications (Paytm)
    sector: Technology / Fintech Software
    thesis: >
      Strong fundamentals (revenue growth +27.7%, earnings growth +79.9%),
      but added 2026-08-24 at an extended entry point — 97.5% of 52-week
      range, RSI 74.7, trailing PE 161. Even the analyst mean target
      (1,558) sits BELOW the current price (1,632) — the near-term
      technical/analyst case argues for waiting, not buying now, despite
      the genuinely strong underlying growth.
    entry_zone_low: 1400
    entry_zone_high: 1550
    current_price: 1632
    strategy: "Wait for pullback toward the analyst mean target / 50-day MA zone (50-MA currently 1,335, 200-MA 1,218) — do not chase at the 52-week high."
    max_position: 15  # shares

  - symbol: GVTD
    icici_symbol: "GE VERNOVA T&D"
    name: GE Vernova T&D India Ltd
    sector: Power T&D / Electricity Infrastructure
    thesis: India power grid expansion, T&D capex supercycle
    entry_zone_low: 3700
    entry_zone_high: 4000
    current_price: 4466
    strategy: "Wait for 15-20% correction. Buy 5 shares per tranche."
    max_position: 15  # shares

  - symbol: POWERINDIA
    name: Hitachi Energy India Ltd
    sector: Power T&D / Electricity Infrastructure
    thesis: Grid modernisation, transformer demand, energy transition
    entry_zone_low: 25000
    entry_zone_high: 27000
    current_price: 33550
    strategy: "Wait for 20%+ correction. At 52W high, don't chase."
    max_position: 3  # shares (high price stock)

# EXIT / REDUCE — data-driven portfolio cleanup
# Phase 1 = act immediately / on bounce. Phase 2 = set limit orders.
exit_triggers:
  # --- Already executed — remove after confirming in ICICI Direct ---
  # GENOVE: sold
  # SUZENE: sold
  # ANARAJ: not in holdings
  # HCLTEC: sold (0 shares as of 2026-07-19 statement)
  # DIXTEC: sold (0 shares as of 2026-07-19 statement)
  # MAZDOC: sold (0 shares as of 2026-07-19 statement)
  # HERHON: sold (0 shares as of 2026-07-19 statement)

  # --- Watch / hold — NOT immediate exits (corrected 2026-08-04, re-verified live) ---
  # Re-verified 2026-08-04 against fresh statement + live Yahoo data. The 2026-07-19
  # review already caught this rationale text as stale but the action string itself
  # was never corrected, so it kept rendering as "Exit immediately" in every report
  # since. Actual: 75 sh @ avg cost 932.52, live price 1042.70 = +11.8% profit (not a
  # loss). SBI is a bank, not insurance. Real earningsGrowth is -3.1%, not -40%.
  # Trader decision (2026-07-19, still valid): hold and watch trend, review ~2026-10-17.
  - icici_symbol: STABAN
    trigger: 0
    shares: all
    phase: 3
    action: "HOLD — thesis intact, +11.8% profit as of 2026-08-04 (not a loss-cut). Review 2026-10-17, do not exit on this trigger."

  # Re-verified 2026-08-04: 45 sh @ avg cost 1226.13, live price 1172.60 = -4.4%
  # (small loss, direction was right). Earnings are worse than the config stated —
  # real earningsGrowth -68.7%, not -14% — which reinforces rather than weakens the
  # "exit on bounce" thesis. Price crossed 1350 at some point in the last 89 days
  # (89d high 1367.03) and has since pulled back below the trigger — if that bounce
  # wasn't acted on, the window was missed; re-arm for the next bounce.
  - icici_symbol: DRREDD
    trigger: 1350
    shares: all
    phase: 2
    action: "Exit on bounce >= 1,350 — pharma not in core theme, real earnings growth -68.7% (worse than originally stated, thesis still holds)"

  # Re-verified 2026-08-04: trigger (414) still sits above the 89-day range (89d
  # high 410.20, low 341.85); stock at 343.65, near its 89-day low again. Share
  # count corrected: actual holding is 125 (not 200 — a 100-share sale already
  # happened 2026-05-21). At avg cost ~375.61, position is -8.5%. Trader decision
  # (2026-07-19, still valid): keep level as-is, watch trend, review ~2026-10-17.
  - icici_symbol: NTPC
    trigger: 414
    shares: 75
    phase: 3
    action: "WATCH — trigger (414) sits above the 89-day range, not close. Actual holding 125 sh (corrected from stale 200), -8.5% unrealized. Review 2026-10-17, do not re-anchor now."

  - icici_symbol: ADAPOW
    trigger: 230
    shares: all
    phase: 2
    action: "Exit >= 230 — take 38%+ gain, re-enter lower (stock ran 108% in 1Y). Live price 209.95, not yet triggered; recent buys at ~208-219 look like averaging, not exit prep."

  # Re-verified 2026-08-04: fixed the trigger-vs-text mismatch (field said 820, text
  # said 750). Live price 845.50 IS above the real 820 trigger — this one is
  # genuinely actionable today, unlike the others in this block. ~150 sh @ avg cost
  # ~782.92 = +8.0% profit.
  - icici_symbol: YATHOS
    trigger: 820
    shares: all
    phase: 1
    action: "Exit all at market — price 845.50 above the 820 trigger, +8.0% profit on ~150 sh. Genuinely actionable now."

  # Re-verified 2026-08-04: trigger (2,700) still sits ~5.4% above the 89-day high
  # (2,561.53), never touched. Share count corrected: actual holding is 100 (not the
  # stale "15 of 30" — averaging continued after the 2026-07-19 review with more
  # buys at 2,279.60 and 2,093.50). At avg cost ~2,524.29, live price 2,460 = -2.5%
  # (improved from -6.2% in July as the newer, cheaper lots pulled the average down).
  # Trader decision (2026-07-19, still valid): keep level as-is, watch trend, review
  # ~2026-10-17 — do not re-anchor or resize now.
  - icici_symbol: TCS
    trigger: 2700
    shares: 100
    phase: 3
    action: "WATCH — trigger (2,700) sits well above the 89-day high, not close. Actual holding 100 sh (corrected from stale 30), -2.5% unrealized. Review 2026-10-17, do not re-anchor or resize now."

